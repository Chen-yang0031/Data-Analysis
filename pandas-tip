在数据分析的案例中，经常会遇到需要对dataframe进行循环遍历、判断和赋值的操作，这种情况下一般有三种做法，直接赋值，loc和at。
直接赋值指直接对dataframe内元素进行赋值，例如pd[column][row] = x，这样的做法并不提倡，因为直接赋值并没有对dataframe直接进行更改，
可能导致赋值失败，一种更安全的做法是df.loc[index,column] = x,这种做法保证了赋值有效性，但是在时间复杂度上不占优势，对于大批量的赋值
效果并不好。最后一种方式相对比较提倡，即df.at[index,column] = x，时间会比loc函数快上百倍。
